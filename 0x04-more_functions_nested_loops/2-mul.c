#include "main.h"

/**
 * mul- multiplies two numbers
 * @a: one of the numbers to be multiplied
 * @b: the second number
 * Return: returns results of multiplication
 */

int mul(int a, int b)
{
        return (a * b);
}
